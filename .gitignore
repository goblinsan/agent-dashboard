# ---- Root repo ignores (FastAPI + React + Docker) ----

# Multi-agent artifacts
.ma/**
!.ma/context/
.ma/context/*
!.ma/context/snapshot.json
!.ma/context/summary.md

# OS cruft
.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.*
.env.local
.env.*.local

# Python (API / FastAPI)
__pycache__/
*.py[cod]
*.so
*.pyd
*.pyo
*.egg-info/
.eggs/
.venv/
venv/
.envrc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache/            # e.g., pip, pytest, etc.

# Alembic (keep migrations, ignore cache/artifacts)
alembic.ini      # if generated locally; commit your template as needed
alembic/*/cache/

# Node / React (web)
web/node_modules/
web/.vite/
web/dist/
web/.eslintcache
web/*.tsbuildinfo
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# IDE / Editor
.vscode/*
!.vscode/extensions.json   # keep workspace recommendations
!.vscode/settings.json     # keep shared settings if desired
.idea/
*.iml
.history/
*.swp
*.swo

# Docker (named volumes are external; ignore local build artifacts)
docker-data/
tmp/
tmp/**

# Redis & Postgres dumps (never commit runtime data)
dump.rdb
appendonly.aof
*.dump
*.sql
*.sql.gz

# OpenTelemetry, coverage, and misc cache
*.cov
coverage-final.json
.nyc_output/
.prisma/                  # if later using Prisma
opentelemetry.log
otel/*/data/

# Build artifacts (generic)
build/
dist/
out/

# Images/exports generated during dev
*.png~
*.svg~

# Lockfiles — KEEP these if present (do not ignore):
#! package-lock.json
#! yarn.lock
#! pnpm-lock.yaml
#! poetry.lock
